import React from 'react';

const Input = ({ ...allProps }) => <input  {...allProps} className="w-full bg-white focus:outline-none border py-2 px-4 rounded-lg shadow-sm focus:border-blue-300"/>;

export default Input;
