
const INITIAL_STATE = {

};

const persistedReducer = (state = INITIAL_STATE, { type, payload } : { type : string, payload : any}) => {
  switch (type)
  {
		
  default:
    return state;
  }
};

export default persistedReducer;

