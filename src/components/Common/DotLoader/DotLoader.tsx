import './DotLoader.scss';

const DotLoader = ({ ...otherProperties }) => (
  <div {...otherProperties}>
    <div className="loader-5 ">
      <span></span>
    </div>
  </div>
);

export default DotLoader;
